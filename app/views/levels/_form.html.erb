<%= form_with(model: [@section, level]) do |form| %>
  <% if level.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(level.errors.count, "error") %> prohibited this level from being saved:</h2>

      <ul>
        <% level.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :level_height, style: "display: block" %>
    <%= form.number_field :level_height %>
  </div>

  <div>
    <%= form.label :level_depth, style: "display: block" %>
    <%= form.number_field :level_depth %>
  </div>

  <div>
    <%= form.label :section_id, style: "display: block" %>
    <%= form.text_field :section_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
